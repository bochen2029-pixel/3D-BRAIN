# NEXT SESSION — START HERE (2026-07-27)

> Read **`HANDOFF.md`** first — it is the self-contained snapshot. Then `CLAUDE.md` (canon, phase
> fence, session protocol — LAW), then the tail of `SESSION_LOG.md`.
> **Trust the files over any recalled summary. Verify before acting.**

## ONE LINE
**Gate B (B1–B9) PASSES** on `run/V_s1234` + 2 more seeds + both B8 directions. The Session-3
mandate to build a desynchronizing mechanism is **WITHDRAWN** — the "void" was two saturated
homeostats, not missing physics. **Do not build a desynchronizer.**

## VERIFY FIRST (2 minutes)
```
python tools\bp_validate.py                    # estimator recovers known m; reads 0 on dead
python tools\analyze.py run\V_s1234            # expect: ALL SEVEN CLAUSES PASS
git -C C:\3D-BRAIN log --oneline -5
```

## THE ONE THING BLOCKING A CLEAN "GATE B PASSED"
**§5.1 — does the sustained frame still owe a power-law statement?** `RELAY_TO_WEB.md` is written
and paste-ready for the web-Claude consult; it needs a human to paste it (the Chrome extension is
not connected, so it cannot be sent from here).

The framing that matters and was found late: **the contradiction is in `FINAL_BLUEPRINT.md`
itself.** §7.4 demands avalanche α≈1.5 / β≈2.0 / crackling / shape-collapse; §2.5 targets the
never-silent reverberating regime in which those statistics are undefined. Both cannot be
required. This is a blueprint-level question, not a `MODULE.md` one.

## THEN, IN ORDER
1. **Transient robustness** — B8 is steady-state by construction and cannot see the finding that
   motivated it (a sudden 2 % pulse collapses exc below a tenth of baseline in ~26 % of trials,
   always recovering). Decide whether that is disqualifying. Needs the consult's view.
2. **ISN paradoxical effect** — TESTED AND POSITIVE (z = -3.7, 1 % perturbation). Proposed as
   clause **B9**; approve or decline it in `CONTRACT_CHANGES_PROPOSED.md`.
3. **Blueprint mechanisms never built** — cross-homeostasis, fast inhibition τ_I<τ_E,
   circuit-breaker. Notable that the AI state was reached *without* them.
4. **Perf methodology** — this machine shows a 3.3× spread on identical binaries. Any perf work
   needs a repeated benchmark on a quiet box first; two claims were withdrawn for want of one.

## DO NOT
- Build a desynchronizing mechanism (the mandate is withdrawn — see `SESSION_LOG.md` Session 4).
- Build **Morton, RT/OptiX, structural growth, real-time render, dendritic/AdEx/Tensor/multi-GPU,
  transpose-CSR** — all fenced to Phase 2+ by `CLAUDE.md`. Refuse and cite the fence.
- Quote a performance number from a single run.
- Report a clean-looking result without first finding the null it should be compared against —
  six such results were refuted this session, every one by a null rather than by inspection.


